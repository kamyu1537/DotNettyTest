syntax = "proto3";

package Protocol.Protobuf;

message Packet {
  oneof Data {
    Ping ping = 1;
    Pong pong = 2;
  }
}

message Ping {
  string channel_id = 1;
  int32 data = 2;
  int64 time = 3;
}

message Pong {
  string channel_id = 1;
  int32 data = 2;
  int64 time = 3;
}